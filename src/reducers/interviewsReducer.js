export const initialState = {
    interviews: [],
    loading: false,
    hasErrors: false,
}

export default function interviewsReducer(state = initialState, action) {
    switch (action.type) {
        default:
            return state
    }
}